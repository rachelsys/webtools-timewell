'use client';
import { useState } from 'react';
import Link from 'next/link';
import { ArrowLeft, ArrowUpRight, Sparkles } from 'lucide-react';

const decide=(input:string)=>{const text=input.toLowerCase();if(text.includes('flight')||text.includes('pack')||text.includes('行李'))return['行動清單',['線上報到並儲存登機證','收好證件、充電器與藥品','設定鬧鐘並確認前往機場的路線','睡前把行李放到門邊']];if(text.includes('quit')||text.includes('job')||text.includes('離職'))return['決策框架',['現在最難以承受的是什麼？','如果六個月後沒有改變，你會後悔什麼？','什麼改變會讓留下來值得？','這週能做哪個低風險實驗？']];if(text.includes('learn')||text.includes('python')||text.includes('學'))return['四週學習路徑',['第 1 週：理解基礎概念','第 2 週：做一個小練習','第 3 週：用真實資料解題','第 4 週：完成一個自己會用的作品']];return['可執行的下一步',['寫下「完成」具體長什麼樣子','選擇今天能試的最小版本','設定 20 分鐘完成第一稿','記下讓你意外的地方']]};

export default function Anything(){const[input,setInput]=useState('');const[result,setResult]=useState<ReturnType<typeof decide>|null>(null);const run=()=>{if(input.trim())setResult(decide(input))};return <main className="anything-archive"><header><Link href="/"><ArrowLeft size={16}/> 回到倒數一下</Link><span><Sparkles size={14}/> anything 實驗</span></header><section><p className="archive-kicker">AN EARLIER EXPERIMENT</p><h1>告訴我任何事。</h1><p className="archive-lede">我會判斷它比較適合變成清單、計畫、框架，或一個可執行的下一步。</p><div className="archive-composer"><textarea value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();run()}}} placeholder="把腦中的東西丟進來…"/><button onClick={run} disabled={!input.trim()}>讓它變得有用 <ArrowUpRight size={18}/></button></div>{result&&<div className="archive-result"><span>{result[0]}</span>{result[1].map((item,i)=><p key={item}><b>{String(i+1).padStart(2,'0')}</b>{item}</p>)}</div>}</section></main>}
